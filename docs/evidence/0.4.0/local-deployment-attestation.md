# Local deployment attestation — implementation checkpoint

Date: 2026-09-07. This is partial recovery release Task 3 evidence, not a release authorization or completed release gate.

## Current source reconciliation — 2026-09-11

The Task 1 schema-2 foundation described by the historical checkpoint below is
now present in the current source. `scripts/recovery-binding-projection.mjs`
owns the fixed wire order, canonical decoding and acyclic commitment/core
projections; `scripts/recovery-activation-candidate.mjs` owns static candidate
validation, commitment generation and V2/V3 dispatch; and
`scripts/generate-0.4.0-recovery-launch-activation.mjs` owns the validated launch
projection. The focused root suites for binding projection, V3 activation and
authorization verification pass 365 tests on the current checkout.

This is local parser/projection/generator evidence only. It does not authenticate
provider metadata, establish live recovery authority, install an activation
binding, deploy a service, or complete any release gate. The remaining work is
the source-bound metadata/attestation command and its authenticated workflow
consumer.

`scripts/generate-warpkeep-deployment-attestation.mjs` derives canonical attestation bytes from actual local files and verifies an installed marker against a fresh capture. Supplied commit/tree/closure coordinates are data only: this module does not authenticate their origin, install an attestation, grant deployment authority, or deploy anything.

The real filesystem tests cover changed/extra content, malformed identities, getters/proxies, forbidden hidden/dependency paths, links, non-ASCII paths, canonical marker bytes, mutation after an earlier file was hashed, and archive budget boundaries. The recovery service archive test passes generated bytes through its actual streamed ZIP/TAR reader and rejects changed content. These are synthetic fixtures, not production artifacts or owner playtests.

Reproduce from the repository with the pinned Node 22 toolchain:

```text
node node_modules/vitest/vitest.mjs run tests/deploymentAttestation.test.ts
node node_modules/typescript/bin/tsc -b
```

Then from `services/release-recovery` with its installed dependencies:

```text
node node_modules/vitest/vitest.mjs run test/archive.test.ts
node node_modules/typescript/bin/tsc --noEmit
```

Latest local run: 23 tests passed, exit 0, 58.53 seconds; root and recovery-service TypeScript checks also exited 0. The archive suite passed 83 tests in 4.30 seconds. The aggregate-path regression first reproduced acceptance of a tree exceeding the receiver's 4 MiB path budget, then passed with the accumulator that reserves the generated marker path. Review found no critical or important production defect within this partial scope; its directory-order test concern was corrected with explicit test ordering and per-test mock reset before these final runs. These results are a checkpoint, not final release verification.

Still required: safe installation and source-bound CLI integration; checks against the exact production packager's long-name entries, root/directory entries, and record padding; final archive verification; authorized workflow integration. Filesystem identity rechecks detect observed traversal changes but do not provide an atomic filesystem snapshot or lock against subsequent writes. The eventual release operation must retain isolation and verify the final packaged bytes.

No production mutations, admissions changes, historical receipts, or deployment claims are supplied by this component. Full Task 3 and the live release remain incomplete.

## Source-bound command prerequisite (2026-09-07)

After checkpoint `ed69c896ac1b2e78bd7b7cdd79d219e1001c1a04`, direct CLI execution explicitly fails rather than returning success without verification. The importable derivation remains data-only.

The following inspection was recorded on 2026-09-07, before the Task 1 source
owners were published; it is retained as historical context and does not
describe the current file inventory. The original dependency was recovery
release plan Task 1, not an arbitrary-hash CLI wrapper:

- `config/releases/0.4.0-sealed-launch.json` is schema 1.
- `scripts/verify-0.4.0-sealed-launch.mjs` has a schema-1-only `parseBinding` and historical v1 receipt commitments.
- The current root owners are `scripts/recovery-binding-projection.mjs`,
  `scripts/recovery-activation-candidate.mjs`, and
  `scripts/generate-0.4.0-recovery-launch-activation.mjs`; their focused tests
  are recorded above.
- `services/release-recovery/src/githubEvidence.ts` already defines the exact schema-2 field order and service-side acyclic projections. Its receipt snapshot excludes commitment slots and forces the core slot to null; the subsequent core includes populated commitments and forces only its own slot to null. Its `githubEvidence.test.ts` exercises that receiver contract with synthetic metadata.

Next implementation: connect the existing root owners to source-bound metadata,
attestation coordinates and an authenticated workflow consumer while preserving
schema-1 behavior. Do not populate the checked-in release binding from synthetic
service fixtures, treat those fixtures as live evidence, or claim the failed CLI
guard completes Task 3.
