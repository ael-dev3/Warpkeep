# Alpha 0.3.43 launch screenshot record

This directory preserves the root README preview captured on 2 August 2026
from the actual Alpha 0.3.43 Realm interface at a 1400×900 CSS-pixel viewport.
The source was Warpkeep's loopback-only rendered WebGL QA fixture with
synthetic keep records. No production account, real FID, private balance,
token, session, or user profile was used or retained.

| File | Purpose | Technical identity |
| --- | --- | --- |
| `warpkeep-alpha-0.3.43-genesis-001.png` | Root README Realm preview | 1400×900 opaque RGB PNG; 1,592,990 bytes; SHA-256 `c4bf41748646fa6753c592ae7e0f2dd51bac39d8bc2e7893b7a868c25c16bc37` |

The browser viewport used device pixel ratio 1. The capture was flattened onto
Warpkeep's dark presentation background and encoded as PNG with Sharp caching
disabled and concurrency limited to one. It is a documentation artifact, not
a production dependency or proof of admission.

The image is `LicenseRef-Warpkeep-Provenance-Required`. Its inclusion does not
independently relicense depicted models, textures, marks, fonts, or Warpkeep's
canonical identity.
