# Hegemony Logging Camp runtime assets

This record pins the three exact owner-supplied Logging Camp GLBs selected for
the distinct draft Wood-node workstream. The supplied delivery directory and
editable masters are not checked in or fetched by routine builds; only the
named, digest-bearing public runtime files are preserved for a separately
reviewed integration.

The supplied manifest declares a +Y-up, +Z-front, ground-plane-at-zero
vertex-color asset with no textures or external dependencies. The render mesh
is not collision, map placement, resource, worker, reward, or SpacetimeDB
authority. Render through a private bounds/terrain-contact wrapper and use the
source-recommended engine-side footprint rather than render-mesh collision.

See [manifest.json](manifest.json) for the exact hashes, source-manifest facts,
LOD budgets, collision guidance, offline installer/verifier, and authorization
boundary.
