# 0.4.0 Linux production runner

## Current dedicated Linux integration

At **2026-09-10 21:20 UTC**, GitHub reported the existing registration ID 22,
`warpkeep-wsl-production-01`, **online and idle**. Its service in `WarpkeepRunner`
was enabled and running. No protected production workflow or provider operation
was dispatched. The earlier Ubuntu/account1001 installation below is historical.

The current caller uses `warpkeep` UID/GID 1000, `/home/warpkeep` mode `0750`,
`/home/warpkeep/actions-runner` mode `0700`, and the fixed Node 22.22.3 toolchain.
The workflow executable at
`/home/warpkeep/.warpkeep/release-preparation-v1/toolchain/node-v22.22.3-linux-x64/bin/node`
was verified: owner `1000:1000`, mode `0500`, 124819136 bytes, SHA-256
`e6ec2c188d83d813f81f2de8aea084d74dce603ac1abedd0a30ad941b10087b2`.

The sealed `audit/private`, `runtime` and `cache` roots beneath
`/home/warpkeep/.warpkeep/private/sealed-realms-v1` were absent and have now been
provisioned empty. Checked directory descriptors, no-follow opens and directory
fsync established owner `1000:1000`, mode `0700`, and no access/default ACL on
the roots and their ancestors under `.warpkeep`. No credentials, receipts, keys
or authorization records were created or migrated. Preflight validates existing
roots; it does not provision them.

Preflight, G001 policy observation and activation inspection have read-only GitHub
permissions; only activation generation receives job OIDC. Actual protected
workflow execution and authenticated provider composition remain open.

## Interrupted service installation repaired — September 10

The earlier installation stopped after writing the systemd unit, before copying
`bin/runsvc.sh`, writing `.service`, or enabling the unit. The installed unit was
verified against the installed official template with the exact account,
description and runner root. The missing helper and marker were created
exclusively, with their bytes and ownership checked before daemon reload/start.
The helper is owner `1000:1000`, mode `0755`; the marker is mode `0600`.
No registration was replaced or requested again. GitHub subsequently reported
the same ID 22 online. `Runner.Listener --version` returned `2.337.0`.

The service uses the runner package's own Node, separately from the attested
workflow Node. Preserve historical private state. Follow
[local preparation](0.4.0-local-release-preparation.md) for native preparation;
runner connectivity alone is not preparation or deployment acceptance.

## Previous CI installation evidence

At **2026-09-08 02:39 UTC**, GitHub reported `warpkeep-wsl-production-01`
online and idle. Its persistent systemd service was enabled and running as
UID/GID 1001. This establishes runner availability; no recovery workflow,
production deployment or live job/OIDC authorization was exercised.

## Historical installed profile — September 8

| Component | Verified value |
| --- | --- |
| Host | `Ubuntu-24.04` under WSL2, Linux X64, native Linux filesystem |
| Runner | `warpkeep-wsl-production-01` |
| GitHub labels | `self-hosted`, `Linux`, `X64`, `warpkeep-production-admin`, `warpkeep-repository-exclusive` |
| Configured group | `Default`, read from the official registration configuration |
| Account | `runner`, UID/GID `1001`, locked password, `/usr/sbin/nologin`, no privileged supplementary groups |
| Home and runtime | `/home/runner`, `/home/runner/actions-runner`; owner `1001:1001`, mode `0700` |
| Recovery state root | `/home/runner/.warpkeep-recovery-v1`; owner `1001:1001`, mode `0700`, initially empty |
| Sealed-operation roots | `/home/runner/Library/Application Support/Warpkeep/operations/{audit/private,runtime,cache}`; separately provisioned, owner `1001:1001`, mode `0700`, verified empty |
| Service | `actions.runner.ael-dev3-Warpkeep.warpkeep-wsl-production-01.service` |
| Installation archive and manifest | `/var/cache/warpkeep-runner`; root-owned private directory and read-only files |

The runner is registered only with `ael-dev3/Warpkeep`. Its private configuration
files have mode `0600`. Keep credentials, RSA parameters, raw diagnostics, job
environment and recovery receipts out of Git, terminal output and public handoffs.
The UID 1000 local release assembler uses a different account and private state.

## Historical package provenance and registration

The installed official package is **2.337.0**,
`actions-runner-linux-x64-2.337.0.tar.gz`, 226430031 bytes, SHA-256:

```text
70920811a4f8ad4328818682bca5c6469c1c942fab52448868071d0063816613
```

The authenticated [repository runner-download API](https://docs.github.com/en/rest/actions/self-hosted-runners#list-runner-applications-for-a-repository)
and the [official release asset](https://github.com/actions/runner/releases/tag/v2.337.0)
returned the same digest; downloaded bytes matched it. The complete 9310-member
package inventory, including six official relative CLI links, was verified.
An offline version check as UID 1001 returned `2.337.0`; native dependency checks
passed without installing OS packages. Its one diagnostic file was preserved
privately and inventoried separately from package files.

Registration used one short-lived token request. The response stayed in memory,
crossed an anonymous stdin pipe, and reached configuration through
`ACTIONS_RUNNER_INPUT_TOKEN`, which the [pinned runner implementation](https://github.com/actions/runner/blob/v2.337.0/src/Runner.Listener/CommandSettings.cs)
masks and removes. No token was passed in command arguments or printed.

The initial local completion check rejected the official `.runner` file's UTF-8
BOM after registration had already succeeded. GitHub metadata and the sanitized
local name/group/URL/work-directory fields established the actual result. The
reader was corrected to accept `utf-8-sig`; no registration request or replacement
was repeated. Treat any future ambiguous setup result the same way: inspect the
existing registration before retrying. Preserve private configuration and state.

Normal GitHub runner updates remain enabled. The installation manifest describes
the initial package, not an updated runner. Workflow runtime attestation remains
separate. Service installation follows GitHub's [supported `runsvc.sh` service entry point](https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/configure-the-application)
and ran as `runner` in the historical installation. The current service runs as
`warpkeep`; it does not execute jobs as root.

## Status and maintenance

From PowerShell, inspect only selected unit fields and sanitized GitHub metadata:

```powershell
$wkRunnerUnit = 'actions.runner.ael-dev3-Warpkeep.warpkeep-wsl-production-01.service'
wsl.exe -d WarpkeepRunner -u root -- /usr/bin/timeout 10 /usr/bin/systemctl show $wkRunnerUnit --property=ActiveState,SubState,UnitFileState,User,Group,MainPID,Result
gh api repos/ael-dev3/Warpkeep/actions/runners --jq '.runners[] | {name,labels:[.labels[].name],status,busy}'
```

An active service can briefly precede GitHub reporting the runner online. Inspect
both states before restarting it. Avoid publishing raw journal or diagnostic
output; inspect failures privately and report only the relevant sanitized cause.

Start an intentionally stopped, healthy installation with:

```powershell
wsl.exe -d WarpkeepRunner -u root -- /usr/bin/timeout 20 /usr/bin/systemctl start $wkRunnerUnit
```

Before stopping for maintenance, confirm the exact runner is idle and no recovery
job or unresolved deployment is using it. If a job is busy or its outcome is
uncertain, inspect that run and its reconciliation state first. Do not delete
receipts, clear locks, replace registration or kill an unrelated process.

```powershell
wsl.exe -d WarpkeepRunner -u root -- /usr/bin/timeout 30 /usr/bin/systemctl stop $wkRunnerUnit
```

The unit is enabled for future distro starts; installation required no WSL
restart. It does not keep Windows awake or ensure WSL launches after Windows
boots. Availability depends on the Windows host, network and running distro.
No Windows startup task or sleep setting was changed. Do not operate another
project's distro or use the historical Ubuntu installation for current maintenance.

## What this enables, and what remains

The recovery production job matching this profile is
[`deploy-pages.yml` → `deploy-recovery`](../../.github/workflows/deploy-pages.yml).
The signer independently checks the real job's name, labels and group through
[GitHub evidence validation](../../services/release-recovery/src/githubOidc.ts).
The sealed-realm workflow has dedicated Linux callers for preflight, G001 policy
observation, activation inspection and activation generation, described below.
Legacy durable Pages and notification production lanes still target macOS/ARM64.
Other realm provider operations remain unsupported by the fixed Linux caller.

A real recovery run still requires:

1. Authenticated recovery-service deployment and readback. The
   [approved recovery design](../superpowers/specs/2026-09-03-warpkeep-0.4.0-recovery-authorization.md)
   permits a separate recovery trust chain, but current
   [bridge facts](../../scripts/sealed-realms-production-auth-bridge-state.mjs)
   still consume a prepared bridge receipt and completed deployment journal.
   Deployment, binding, import and owner attesters in the
   [activation entry](../../scripts/sealed-realms-production-activation-workflow-entry.mjs)
   remain unavailable. Integrate authenticated producers through a supported
   prepared chain or an explicit recovery chain retaining source/configuration/
   target invariants. Offline bootstrap and changing runner labels cannot supply
   this missing evidence.

2. The complete generated source/bundle family installed in the reviewed source,
   plus an authenticated schema 2 recovery activation binding. The current
   [canonical recovery-candidate reader](../../scripts/sealed-realms-production-recovery-candidate.mjs)
   derives authenticated Git/bootstrap/corpus facts and supports completed
   historical inspection. Genuine recovery, Worker, module and approval producers
   remain incomplete. A prepared directory or fixture JSON cannot replace those
   facts and the genuine receipt chain.
3. The actual recovery gateway/private signer, keys, durable ledger, observer
   binding and release-specific authorization state. Checked-in
   [signer configuration](../../services/release-recovery/wrangler.signer.toml)
   remains disabled. Creating the private directory grants no authorization;
   the workflow obtains and persists its verified claim for the exact run.
4. Successful `Verify` for a push of the selected current protected `main`,
   classification into `sealed-g002-recovery`, exact artifact attestation,
   fresh claim and deployment-boundary checks, then mandatory live postflight.
   GitHub CLI/browser login is neither job OIDC identity nor Cloudflare authority.

## Supported sealed operations

[`sealed-realms-production.yml`](../../.github/workflows/sealed-realms-production.yml)
retains manual dispatch, protected current `main`, environment
`notification-bridge-prepared`, and the shared non-cancelling production lock.
`preflight`, `g001-policy-observe` and `activation-evidence-inspect` run in
`operate_readonly` with Actions/content reads. `activation-evidence-generate`
runs in `operate` with job OIDC. Select the exact source only after its prepared
family and required main Verify evidence are valid. Other operations fail
explicitly before their unsupported provider work.

The September 10 CLI repair uses the same operation map for input and dispatch.
Previously the executable rejected `g001-policy-observe` even though the workflow
and exported dispatcher supported it. Executable regression tests now verify all
four supported operations reach host attestation, and unknown operations and
malformed arguments fail at input parsing. This is not a protected dispatch result.

The workflow verifies the real installed account and roots, clean committed
bootstrap files, fixed Git executable, and exact Node 22.22.3 Linux/X64 bytes.
It executes the fixed installed Node toolchain, rejects ambient
runtime overrides and passes the GitHub token only through the inherited
environment. No dependency installation occurs. The
[fixed caller](../../scripts/sealed-realms-production-linux-preflight.mjs) then
authenticates prepared ancestry, the complete selected G001 source graph,
bundle/declaration and actual attested import before invoking the existing G001
factory and operation. It reopens source and runtime identity around both awaits.

`preflight-inspected` establishes source, workflow and private-root inspection.
It does not establish provider readiness, signer authorization, player acceptance
or launch readiness. A failure reports only a bounded phase; retain actual job
identity and inspect the relevant canonical checks rather than overriding a hash,
path, account, source or factory to force progress. Git or Node upgrades require
a reviewed runtime-policy change with matching verification.

## Historical private-root and fixture evidence — September 8

The sealed roots were absent at the first September 8 inspection. They were
created separately as UID/GID 1001 using checked directory descriptors, no-follow
opens, mode `0700` and directory fsync. Every ancestor under the account home
was validated, with no access/default ACL; all three final roots were empty.
No credentials, receipts, keys or authorization records were created, and no
existing root was repaired. Runtime preflight continues to validate existing
roots only; it never provisions them. The account's `nologin` shell is intentional:
maintenance uses a direct `runuser -u runner -- <fixed executable>` invocation
from the administrator context, rather than enabling an interactive account.

The earlier native tests executed the real bundled caller under UID/GID 1001 in a private mount
namespace with synthetic read-only GitHub responses. Workflow tests exercise its
actual shell guards and source/token transport. These establish local composition,
not a successful protected dispatch. See dated
[engineering evidence](../evidence/0.4.0/release-engineering.md) for the exact source
and test scope. A new combined source must undergo its own native preparation and
independent check before operating.

Continue through the [release checklist](0.4.0-release-checklist.md),
[recovery service boundaries](../../services/release-recovery/README.md) and
[release-engineering evidence](../evidence/0.4.0/release-engineering.md).
Do not dispatch a recovery workflow solely to test runner connectivity.
