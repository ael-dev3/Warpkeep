# Warpkeep 0.4 infrastructure access

Maintained 2026-09-25. **Access guide and resumption procedure.**
Use alongside the release checklist and local-operations evidence. No credentials
belong in the release package. Reverify targets before every production operation.

## Hosting boundaries

- Frontend: GitHub Pages, https://warpkeep.com/.
- Auth bridge: Cloudflare Worker `warpkeep-auth-bridge`, https://auth.warpkeep.com/.
- Persistent realms: SpacetimeDB Maincloud; `warpkeep` (G001),
  `warpkeep-genesis-002` (sealed G002), `warpkeep-ptr` (isolated owner PTR).

Provider login is distinct from in-game owner authentication. Successful CLI
listing does not prove owner admission, possession of historical signing keys,
or that release-specific receipt and recovery requirements are satisfied.

## Resume without guessing

Start at the [execution handoff](../agent-notes/0.4.0/execution-handoff.md), then
inspect the actual [checkout role](0.4.0-development-sync.md#start-from-the-actual-checkout-and-remote),
live branch/PR and active process. Historical failure text is a lead to verify,
not a current blocker. Diagnose only the boundary needed for the next action.

| Boundary | Where to connect or inspect | Evidence needed before calling it blocked |
| --- | --- | --- |
| GitHub source and checks | Existing `gh`/Git access to `ael-dev3/Warpkeep`; the maintained checkout's actual remote | Live ref, PR head, exact run event/status and failing job/step. A pending run or a superseded cancellation is not failed authentication. |
| Protected commit signing | GitHub `main` required-signatures policy, PR mergeability, and the existing SSH signing key on the maintained Windows host (`C:\Users\heyas\.ssh\warpkeep_signing`) | GitHub's exact merge-block reason and commit verification for every PR commit. Confirm the existing key's public fingerprint against the account's registered signing keys; do not create an App/recovery key, disable protection or use `--admin`. Follow the [signing recovery procedure](../agent-notes/0.4.0/execution-handoff.md#22-september-2026--protected-commit-signing-recovery). |
| Native execution | Existing `WarpkeepRunner`, user `warpkeep`, and `warpkeep-wsl-production-01` Actions runner | Distinguish manual source checkout from Actions workspace, active process from stale journal, and offline runner from failed operation. Follow the [Linux guide](0.4.0-linux-runner.md). |
| Cloudflare provider | Existing `warpkeep-production` profile bound to the original checkout; commands below | Selected account plus the exact Worker operation. A successful deployment list proves read access only; wrong-default-profile results do not diagnose production access. |
| SpacetimeDB publisher | Owner's [ael-dev3 profile](https://spacetimedb.com/@ael-dev3); Windows CLI and the fixed Linux CLI config below | Compare `login show` with the owner profile, then list Maincloud and compare immutable realm identities to source. An empty list for another identity does not mean realms are absent. |
| G001 administrator observation | Fixed native census/policy operator and the existing protected environment secret `WARPKEEP_PRODUCTION_ADMIN_TOKEN` | Both `g001-policy-observe` and `g001-freeze-census` use the application token obtained from the Warpkeep auth bridge. The fixed workflow injects it only into the native execution step; ingress scrubs the environment entry, and native execution keeps the descriptor-backed file inside the private per-operation root until cleanup. No runner-persistent `admin-token` file or additional recovery key is required. A publisher CLI token has a different issuer: `INVALID_ISSUER` from a direct CLI admin procedure does not diagnose publisher login or the bridge secret. |
| Player/owner session | Existing Farcaster owner flow into isolated PTR through the auth bridge | Actual owner exchange/session and target admission. GitHub, provider login and an administrator token do not supply player identity. |
| Private release/recovery authority | Fixed private roots and guarded readers in the [Linux guide](0.4.0-linux-runner.md); existing recovery bootstrap in the [infrastructure audit](../agent-notes/0.4.0/release-and-infrastructure.md) | Identify the exact required receipt/key/binding and its authenticated producer. An empty sealed-state directory does not prove the recovery key is absent elsewhere. Inspect references/metadata without printing secret contents. |

For protected operations, use this order:

1. Verify the intended operation exists in the fixed workflow/caller and maps to
   the correct job. A missing implementation is agent work, not an owner login task.
2. Verify the selected main commit has a matching prepared family and completed,
   successful **push-to-main Verify**. Recheck main after waiting. Follow the
   [M1/M2 integration order](0.4.0-release-checklist.md#fixed-sequence); PR success
   alone and interim source-only M1 are insufficient.
3. Check only the credentials/private authority that this operation consumes.
   Preflight deliberately receives no provider secrets. Repository and
   environment variable/secret scopes differ; an empty environment list does not
   establish absence at repository scope. Secret names do not prove valid values.
4. Run the supported inspection once, capture its bounded result and exact failed
   stage, and use the owning reader/runbook to decide the next action. Distinguish
   source/toolchain refusal, provider denial, player denial and missing private
   evidence. Do not infer a precise cause from a generic `phase:"workflow"` alone.
5. After a repair, repeat the check that demonstrated the problem and update its
   canonical record. Preserve earlier evidence as dated history; stop presenting
   the old failure as current. Do not weaken a guard or invent evidence to pass it.

### Read the reported failure stage

The fixed Linux caller emits only a bounded failure code and phase. Use the
phase to narrow inspection; it does not prove a particular credential is missing.

Native G001 failures can also include a fixed `diagnostic` label. For example,
`g001-materialization` identifies the credential-free build, whereas
`g001-credential-descriptor` identifies opening the private credential after
authority refresh. Labels also distinguish host, source, closure, private roots,
prepared verification, observation, receipt and cleanup. They never include raw
exceptions, paths, provider responses or credential values. A label names the
failed stage; it does not prove a missing credential, rollback or lack of effects.

| Phase | Investigate first |
| --- | --- |
| `input` / `runtime` | Supported operation, matching job, runner/UID, environment and pinned executable. |
| `source` / `bundle` | Exact current main, clean authenticated bytes, compiler-input ancestry and complete generated family. |
| `workflow` | Construction of workflow authority: exact completed Verify, job evidence and required private inputs. Inspect the actual prerequisite; do not infer it from the phase alone. |
| `operation` | The selected operation function was entered. It may still be validating evidence or may have submitted work. Reconcile through that lane's existing private journal/continuation and supported readback before retrying. |
| `result` | The operation returned but its public result or subsequent attestation failed. Treat provider effects as unresolved until the supported reconciliation establishes them. |

Keep raw provider errors, credential material and private records private. A
sanitized failure is not proof of rollback, and a successful preflight is not
proof of deployment or mutation authority.

The protected Actions checkout is intentionally detached at the selected main
SHA. Native G001 readers use `attestPolicySource` to verify clean source bytes,
the matching `origin/main` commit and their operator. Do not route them through
the historical bootstrap checker that requires a local branch named `main`.
That mismatch rejects the checkout before a session or provider request exists.
Workflow duration alone does not establish that either census sample was taken:
materialization also takes time. Only authenticated retained census records and
the supported result establish observation progress.

## Known blockers and recovery paths

Use this index for the failures encountered during 0.4 preparation. These are
diagnostic paths, not additional approval requirements. Match the exact failed
step and source before applying a repair. The agent owns each action below
unless a real interactive login or unavailable historical record is demonstrated.

| Observed symptom | Recovery path | Proof that the issue is resolved |
| --- | --- | --- |
| `SEALED_REALMS_LINUX_AMBIENT_OVERRIDE_INVALID` appears in `Attest runtime and execute authenticated operation`, after that run already passed `Require installed account and private state` | Preserve the exact run, attempt, source and failed step. Confirm the marker came from the post-checkout duplicate environment loop; do not repeat the protected operation or inspect private state. The observed duplicate check was removed in [PR #362](https://github.com/ael-dev3/Warpkeep/pull/362). Do not reapply it. Keep the pre-checkout runner gate, use fixed absolute tools and `env -i` for Git, and clear every non-allowlisted variable before starting Node. Follow the [execution handoff](../agent-notes/0.4.0/execution-handoff.md) for exact-main verification, native prepare/check and generated-only M2. | Focused shell/workflow tests prove both that the initial runner gate still rejects ambient overrides and that post-checkout transport removes injected keys before fixed Node starts. The exact M2 passes Verify and CodeQL before any fresh protected operation. Preserve the original run; only create a new operation identity after its exact prerequisites succeed. |
| Missing GitHub App PEM or failed browser download | Use the [existing workflow-token path](0.4.0-recovery-bootstrap.md#no-additional-recovery-keys). Do not generate another key. Supply the job's `GITHUB_TOKEN` only to the fixed gateway; independently verified OIDC supplies workflow identity. | The actual gateway/Worker RPC tests pass without App credentials; the protected signed observation succeeds after deployment. Browser login alone is not this proof. |
| `INVALID_ISSUER` during a direct CLI admin call | Use the native G001 operator, which obtains the application token from the auth bridge. Check publisher login separately only if a publisher operation fails. Do not log out a working publisher to fix an application-token issuer mismatch. | The supported census/policy operator completes its authenticated read with matching immutable realm identity. |
| Wrong account, empty realm list or wrong Cloudflare profile | Follow the provider commands below with the existing production profile and fixed Linux CLI config. Compare selected account and immutable targets before reauthentication. | Correct account and exact target readback; secret names or a browser session are insufficient. |
| Detached Actions source rejected before a G001 session opens | Check `genesis001-linux-census-operator.ts` and `genesis001-policy-observation-receipt.mjs` use `attestPolicySource`. Preserve detached checkout and exact `origin/main` matching; do not force-create a local `main` branch. | Native descriptor tests and a clean detached source check pass; the operation reaches the actual observation boundary. |
| `g001-policy-observe` fails at `g001-credential-descriptor` | Check that `.github/workflows/sealed-realms-production.yml` allowlists `g001-policy-observe` for the existing protected `WARPKEEP_PRODUCTION_ADMIN_TOKEN`, and that `sealed-realms-production-g001-workflow-entry.mjs` passes it to `prepareFixedLinuxG001PolicyObservation`. Do not create a persistent runner token file or generate another GitHub App key. | The focused workflow/native lifecycle tests pass and a fresh exact-main policy observation proceeds beyond descriptor acquisition. The secret remains scoped to the fixed execution step and its private operation root. |
| Historical `g001-observation` on pre-PR-354 main | Run 35937948183 was bound to M2 `080d91aa`; it failed closed with a generic diagnostic before the source repair in PR #345. That repair is merged and this source is superseded. Do not retry or treat it as current evidence; follow the [execution handoff](../agent-notes/0.4.0/execution-handoff.md) for the current signed M1, its exact-main checks and next preparation step. | Preserve the source-bound attempt; current policy authority requires a fresh verified receipt from an exact protected M2. |
| Historical G001 observation failed before child-stage diagnostics were available | Earlier M3 failures used source that had not implemented privacy-safe diagnostic projection. The original repair merged through [PR #335](https://github.com/ael-dev3/Warpkeep/pull/335), its generated M2 through [PR #336](https://github.com/ael-dev3/Warpkeep/pull/336), and later observer cleanup/session diagnostics through [PR #345](https://github.com/ael-dev3/Warpkeep/pull/345). Do not reapply those completed repairs; use the current execution handoff for the latest source-bound recovery path. | Preserve exact source/attempt identities and select only the fresh exact-M2 result. No additional key or credential path is needed. |
| Supported private-state inspection confirms an active policy receipt is bound to an older source commit | Preserve the owner-only fixed record. Inspect only through the supported private-state API, and follow this path only when it returns a canonical receipt with an older source SHA. Do not delete or edit it and do not request a new key. The existing recovery path archives a verified stale record byte-for-byte under `activation-evidence/records-history/` before reusing the active path; a same-source record is adopted only after authority and receipt verification. It also accepts bounded completed lifecycle history while rejecting duplicate, malformed or incomplete state. Merge any source-only repair through protected M1, prepare/check that exact source, promote the generated M2, and repeat exact-main Verify, preflight and read-only policy observation. | The prior receipt remains recoverable; a fresh exact-M2 operation returns `completed` with a validated current-source receipt or one fixed safe G001 diagnostic. Census/deployment stays gated until observation succeeds. |
| Policy observation returns `g001-policy-state` | The seven-field read-only G001 policy did not match its pinned contract. Compare only those fixed fields through the authenticated read-only operator and exact target; preserve the freeze and avoid automatic writes. Repair the owning source/deployment or live policy only after identifying the mismatch and its authorized owner. | Exact policy matches the pinned receipt contract and a fresh exact-source read returns `completed`; no unrelated policy or player state was changed. |
| Policy observation returns `g001-policy-procedure` | The pinned production target did not expose `genesis_001_access_policy_v1`. Confirm immutable database identity and deployed procedure/schema version; do not substitute a different realm or mutate schema as a diagnostic. | The expected read-only procedure exists on the intended immutable G001 target and exact-source observation completes. |
| Policy observation returns `g001-policy-transport` | The protected read could not complete its bounded auth-bridge/database session. Inspect the exact failed step and current service health using supported read-only checks; make one targeted retry only after a transient cause is established. | A fresh bounded observation completes against the pinned target. A failed session does not prove a provider-side mutation. |
| Policy observation returns `g001-policy-budget` | An unclassified or newer owner-scoped admin-token ledger error occurred. Use its bounded `inspectProductionAdminTokenBudget()` API on the dedicated account; preserve the ledger and let its lock/recovery path reconcile state. It returns counts only. Do not edit or delete ledger files, or create another key. | The inspector succeeds with bounded counts and a fresh exact-source observation completes or returns one of the more specific fixed diagnostics below. |
| The same generic budget diagnostic returns again after safe inspection and lease expiry | On M2 `b7d729b0e8a58cf43870d0baafdf8d8da508fc99`, runs [36060050219](https://github.com/ael-dev3/Warpkeep/actions/runs/36060050219) and [36060910719](https://github.com/ael-dev3/Warpkeep/actions/runs/36060910719) both returned only `g001-policy-budget`. The bounded inspector succeeded, reconciled the expired lease, and showed full capacity before the one retry; the retry still failed. Stop same-source retries. Continue only after a source M1 and generated M2 expose a more specific fixed diagnostic, then follow that category below. Never inspect raw state or count this observation as passed without its verified source-bound receipt. | One exact-source observation returns `completed` with its verified receipt, or one safe subcategory identifies a targeted repair. |
| Policy observation returns `g001-policy-budget-capacity` | The six-attempt bridge-token window is committed by recorded attempts or reservations. Use the bounded inspector and wait for the provider-time window to age out; do not retry until it reports capacity. | A fresh inspection reports an available slot before any new operation. |
| Policy observation returns `g001-policy-budget-clock` | The owner-scoped ledger rejected its clock as invalid or moving backward. Compare the dedicated runner's bounded time against the bridge-provided HTTPS `Date` read. Do not rewrite `lastObservedAtMs` or weaken rollback checks. | A fresh observation uses monotonic trusted time and the ledger inspector succeeds without a rollback. |
| Policy observation returns `g001-policy-budget-reservation` on a source that predates the read-only transport repair | Preserve the attempt, wait for lease expiry, then use the bounded inspector to reconcile only a dead owner's expired lease. Never copy reservation IDs between runs. This recovery was exhausted on the superseded M2; do not repeat it on that source. | The new source's fixed G001 read-only session records each token atomically without an owner-bound reserve; write-capable sessions still reserve. A fresh exact-M2 observation completes and a later census returns its validated receipt. |
| G001 census returns `g001-policy-budget-reservation` on superseded M2 `edb37f40` | Runs [36087746335](https://github.com/ael-dev3/Warpkeep/actions/runs/36087746335) and [36088300562](https://github.com/ael-dev3/Warpkeep/actions/runs/36088300562) failed before the read-only transport repair reached protected main. Preserve both private attempts; do not inspect raw census/ledger state or retry that M2. The repair merged in PR #357 and the generated family in PR #358; exact-main policy observation on `b409b3ae` now passes. | Historical failure remains preserved; current operation proceeds only on an exact verified M2. |
| G001 census returns `G001-ADMITTED-STATUS` | Census run [36116125925](https://github.com/ael-dev3/Warpkeep/actions/runs/36116125925) failed closed on exact M2 `b409b3ae`. Preserve the attempt; do not inspect applicant/census rows or retry it. The safe schema-metadata inspection confirmed `admin_get_access_request_admission_status_v1` is absent on live G001 while the existing read-only `admin_get_access_request_reset_status_v1` export is present. Both return the same fixed five-field status contract. Signed source checkpoint [`2fd0a290`](https://github.com/ael-dev3/Warpkeep/commit/2fd0a290603320ae0c302996091b1a6bbd7c6725) changes only the typed fallback census caller, its focused tests and sealed-launch source checks; 288 focused tests, typecheck, build and the clean verifier pass. No G001 mutation or new key is needed. PR #359 is now protected M1; after exact-main checks pass, prepare and independently check it, promote its authenticated generated family as M2, require exact-M2 Verify/CodeQL, fresh preflight and policy observation, then use one new census identity. | The fresh exact-M2 census returns a validated source-bound receipt, or a new fixed diagnostic identifies the next safe repair. Keep the failed attempt intact. |
| Policy observation returns `g001-policy-budget-lock` | The bounded ledger lock was busy, invalid, replaced, or could not be safely recovered/removed. Wait for the owned lock recovery path and inspect counts again; never unlink or repair lock files by hand. | Inspection succeeds after safe lock recovery, with no overlapping operation and no manual file changes. |
| Policy observation returns `g001-policy-budget-ledger` | Ledger location, permissions, encoding, schema, or durable write validation failed. Preserve the private state and use the bounded inspector only; do not chmod, edit, or delete ledger files. | The inspector succeeds under the dedicated account and the next exact-source observation settles its reservation. |
| Policy observation returns `g001-policy-budget-interrupted` | The owner operation was interrupted before its ledger update became durable. Preserve the run; allow its lease and exact-owner recovery path to settle before a single retry. | The bounded inspector reports no stale reservation and sufficient capacity; a fresh operation completes. |
| Policy observation returns `g001-policy-budget-combined` | Both a ledger mutation and its lock cleanup failed. Do not retry or touch files. Use the bounded inspector after the operation has exited and its lease/lock can be safely recovered. | The inspector succeeds after built-in recovery and reports counts only; then run one fresh exact-source observation. |
| Prepared Linux bridge deploy returns `AUTH_BRIDGE_PREPARED_LINUX_RUNNER_MANIFEST_PIN_INVALID` | Compare the workflow's `WARPKEEP_PREPARED_LINUX_INSTALLED_TOOLCHAIN_MANIFEST_SHA256` with the SHA-256 of `scripts/auth-bridge-notification-prepared-installed-toolchain-linux-x64-v1.json`. The runner must validate the workflow pin against those checked-in bytes; do not hand-edit the manifest or maintain a second stale literal. | The regression test proves workflow-pin/file equality and the runner validates that same file before any provider call. A fresh exact-source run passes runner attestation. |
| Policy observation returns `g001-policy-credential` | Check the existing `WARPKEEP_PRODUCTION_ADMIN_TOKEN` environment scope and its protected execution path. The label may cover descriptor/secret validation or competing credential authority; correct only the existing configured input through its owner UI if proven invalid. Never request its contents in chat or create another key. | The existing protected input passes descriptor validation and the read-only observation completes; no credential is printed or persisted on the runner. |
| Policy observation returns `g001-policy-authority` | Recheck exact source SHA, protected workflow operation/job binding, fixed target and sealed bootstrap profile. Treat any mismatch as source/workflow setup, not a request to broaden credentials or override the target. | The exact signed source and fixed workflow binding are attested and a fresh observation completes. |
| Policy observation returns `g001-receipt` | Check the canonical result/timestamp and source-bound receipt validation for the exact run. Preserve the raw receipt only in its existing private store; do not hand-edit or synthesize one. | A fresh exact-source operation returns a canonical, source-bound receipt and its existing verifier accepts it. |
| g001-freeze-census returns g001-observation after exact-source policy observation succeeds | Run 35839901590 failed generically after creating only partial owner-private first-sample export/proof files; no validated sample or complete receipt exists. Preserve that attempt and never select or retry it. PR #342 merged fixed, privacy-safe stage diagnostics at `a4852b1`; main Verify 35847251543 must finish first. Then prepare/check that exact source, promote and verify its generated-only M2, and run fresh exact-source preflight and policy observation before a new census identity. | The fresh protected census returns a complete source-bound result with two retained stable samples or a specific privacy-safe stage/identity/reconciliation label. Never treat partial private files as a completed baseline or read/copy/post the applicant list. |
| G001 materialization fails finding SDK/compiler modules | The locked Genesis 002 builder owns `spacetimedb/genesis002/node_modules`. Inspect its successful output and the materializer's dependency path; do not install an unrelated parent dependency tree. | The native build completes through the same workflow caller, before opening the credential descriptor. |
| Bash reports `unexpected EOF` or mismatched parentheses | Parse each YAML `run` body with Bash `-n`; inspect quoting of command substitutions and array expansions. Repair the workflow source before retrying a production job. | The shell regression in `authBridgeNotificationPreparedLinuxWorkflow.test.ts` passes and the live job proceeds past source attestation. Syntax failure before the provider step is not missing credentials. |
| GitHub OIDC lacks `check_run_id`, or normal API metadata is rejected | Derive the check identity from authenticated job metadata; validate a supplied optional claim against it. Inspect real response shape and correlate repository, source, run and attempt. Keep full-response fixtures, including nested metadata and attempt-specific URLs. | OIDC/evidence regression suites accept normal metadata and reject substituted identities; the real signed workflow request succeeds. Never fabricate an absent claim. |
| Preparation policy error, or initial gateway `/status` returns 503 | Follow [initial signer deployment](0.4.0-recovery-bootstrap.md#initial-signer-deployment): prepared bridge first, disabled signer with existing secrets and exact policy, then gateway. The initial entry exposes preparation/observation only. | Use protected `ptr-state-inspect` for the first signed live probe. `/prepare` and `/preparation-observation` additionally require the deploy-owned policy; a dry build alone is insufficient. |
| Recovery retry fails or its result is unknown | Preserve the original attempt directory and signed claim. Rerun the same Pages run through its reconciliation step; inspect its retained lifecycle before another mutation. Empty or missing files do not prove no remote request occurred. | The original claim receives a verified terminal outcome; successful resume skips build, upload, claim and deployment. Ambiguity stays unresolved rather than allowing a duplicate deployment. |
| Source/bundle refusal after a repair or squash | Complete source M1 integration, native prepare/check on that exact merged source, generated M2 promotion and successful exact-main Verify. Use the generators; do not type hashes or reuse an older compiled family under a new SHA. | Source and generated-family verification plus the exact protected preflight pass. A successful PR or source-only M1 is not sufficient. |
| WSL commit fails at a `.git` reflog, or closure policy rejects a raw member despite matching Git bytes | Follow the [exact-path ownership recovery](0.4.0-local-release-preparation.md#recover-exact-path-wsl-ownership-and-signing): inspect the failing reflog/member paths and parents, repair only confirmed root-owned paths as root after the owning process exits, then run again as `warpkeep` UID/GID 1000. Configure a local `gpg.ssh.allowedSignersFile` with the registered public signer for SSH signature verification; never expose the private key. | The affected raw files match their committed blobs, closure policy passes, and the signed commit verifies locally and on GitHub. Private release state and unrelated ownership remain untouched. |
| Scanner prints Git errors, then zero commits and “no leaks” | Follow the [resolved-SHA publication procedure](0.4.0-development-sync.md#review-verify-and-form-the-checkpoint). Verify both range endpoints exist before scanning and inspect diagnostics/count as well as exit code. | A nonempty outgoing range was actually scanned without Git errors before the explicit non-forced push; remote SHA then matches. |
| CI is pending or an older PR run was cancelled | Inspect the latest PR head and its actual required checks. Superseded runs do not establish current failure. Avoid restarting unchanged checks or creating commits solely to refresh status. | All required checks pass for the final head; deployment operations additionally require successful push-to-main Verify for their exact source. |

Record a new blocker in the existing execution handoff with: operation, checkout
and commit, exact run/step, bounded diagnostic, whether effects are absent or
unresolved, the owning repair, and the next confirming readback. Add a focused
regression for a demonstrated code defect. Before closing it, reproduce the
original failure, exercise the fixed caller and review the final change. This
prevents a generic failure from becoming another unnecessary credential request.

The two existing Desktop handoff files are entry links only. Their older text
contains known character corruption, including identifiers. Use the maintained
repository notes and live refs; do not reverse substitutions or copy historical
hashes into commands. Update those existing files in place without making siblings.

## Ask the owner efficiently

Use existing session authorization and authenticated browser sessions first.
The agent handles routine inspection, source fixes, synchronization, CI diagnosis
and supported setup. The owner has authorized completing and shipping 0.4,
including repository writes, merges, required service configuration and deployment.
Continue those actions through the existing authenticated write paths; do not
ask for another general approval or treat an inspection command as the limit of
that authorization. Ask the owner only for an action that remains unavailable:

| Confirmed obstacle | Smallest useful human request | Agent's success check |
| --- | --- | --- |
| Correct account is known, but interactive login/MFA is required | “Please finish the open login for account X in tab Y.” Set up the real supported flow first. | Read back CLI identity, exact target listing and required local credential metadata. |
| Selected account differs and no verified owner session is available | “Which account owns these existing realms, or is that session on another device?” | Compare the selected identity and immutable targets after supported login. |
| A required historical private record cannot be found in the known roots | Name the missing record and ask which original machine/private location holds it; request a location, not its contents. | Authenticate the retained record through its existing guarded reader and check source/target binding. |
| Actual-owner or physical-device acceptance is ready | Give one short, prepared test, its URL, expected result and the specific observation needed. | Record the real result and distinguish it from fixture/emulation evidence. |

Each request should contain: the exact blocked action, what was checked, the
single missing human step and how the agent will verify it. Do not ask vaguely
for “all credentials,” ask for passwords/tokens/keys in chat, or repeatedly ask
for an action the owner already completed. Browser login alone does not update a
CLI session; complete its supported flow and confirm the readback.

If the response is pending, continue unrelated authorized work. If blocked work
must be handed off, record its owner and next action in the existing execution
handoff, without creating a new Desktop document or scheduling a new task.

## GitHub: source updates and release inspection

Use the existing authorized GitHub CLI/Git login for commits, pushes, protected
merges and workflow dispatch. Recovery evidence reads use the active workflow's
short-lived `GITHUB_TOKEN` together with separately verified GitHub OIDC identity.
Do not request or generate an additional App key. The legacy Warpkeep Release
Recovery GitHub App remains an optional compatibility path; it does not restrict
the agent's repository or deployment access. See the
[no-additional-key workflow](0.4.0-recovery-bootstrap.md#no-additional-recovery-keys).

The following commands inspect release state without deploying:

```powershell
gh api repos/ael-dev3/Warpkeep/pages
gh pr list --repo ael-dev3/Warpkeep --state open
gh api repos/ael-dev3/Warpkeep/actions/runners
gh api 'repos/ael-dev3/Warpkeep/deployments?environment=github-pages&per_page=3'
```

For a returned deployment ID, read its `statuses_url`. Follow its actual Actions
run and artifact metadata to establish source and artifact provenance. An expired
artifact's recorded digest is not recoverable bytes or a usable rollback package.
Never treat local workflow emulation as GitHub Actions/OIDC identity.

## Cloudflare: select the production profile explicitly

The verified repo-local Wrangler version is 4.110.0, run with Node 22.22.3.
On the current Windows host, use the existing bound checkout and pinned executable
explicitly. A PATH `node` or a different working directory can select the wrong
runtime/profile. These are read-only commands:

```powershell
$wkNode = 'C:/Users/heyas/Documents/Codex/2026-08-11/pl/Warpkeep-0.4.0-worktree/.git/ci-node-22.22.3/node.exe'
$wkCloudflareCheckout = 'C:/Users/heyas/Documents/Codex/2026-08-11/pl/Warpkeep'
& $wkNode --version # Must report v22.22.3.
Push-Location -LiteralPath $wkCloudflareCheckout
try {
    & $wkNode services/auth-bridge/node_modules/wrangler/bin/wrangler.js --version
    & $wkNode services/auth-bridge/node_modules/wrangler/bin/wrangler.js auth list
    & $wkNode services/auth-bridge/node_modules/wrangler/bin/wrangler.js deployments list --profile warpkeep-production --name warpkeep-auth-bridge --json
    if ($LASTEXITCODE -ne 0) { throw 'Inspect the exact Cloudflare read failure.' }
} finally { Pop-Location }
```

Confirm the Node executable/version before these commands. Do not run package
installation against the current shared dependency junction. Setup in a fresh
isolated checkout must use the repository's pinned dependency/toolchain process.

Production account: `c647739b4535010b0b13e6fc296e2c4e`. The default profile in the
0.4 worktree currently selects another account and must not be used implicitly.
The existing `warpkeep-production` profile is bound to the original `Warpkeep`
checkout; it can be selected explicitly for supported operational commands.
In Wrangler 4.110.0, `whoami --profile` is unsupported. To inspect its identity,
run `whoami --json` in the already bound checkout, or use `--cwd` with that
checkout's actual path. Do not switch global profiles just to run a check.

Profile storage is under `%APPDATA%/xdg.config/.wrangler/config/`, with encrypted
credential material protected by Windows Credential Manager. Reference this
location; never copy profile files, export tokens or include logs in the package.
If authentication fails, use the supported login/profile reauthentication flow
with the owner. Do not extract keys or substitute an unrelated account.

Public discovery is available at
https://auth.warpkeep.com/.well-known/openid-configuration and public keys at
https://auth.warpkeep.com/.well-known/jwks.json. These expose public metadata,
not private signing keys, player records or proof of owner login.

## SpacetimeDB: identities and schema inspection

The verified Windows executable is
`%LOCALAPPDATA%/Programs/SpacetimeDB/2.6.1/spacetime.exe`. Check the actual
installation rather than assuming a historical alternate CLI path exists.

```powershell
$warpkeepSpacetime = Join-Path $env:LOCALAPPDATA 'Programs/SpacetimeDB/2.6.1/spacetime.exe'
& $warpkeepSpacetime --version
& $warpkeepSpacetime login show
& $warpkeepSpacetime server ping maincloud
& $warpkeepSpacetime list --server maincloud --yes
& $warpkeepSpacetime describe --server maincloud --no-config --yes --json warpkeep-ptr
```

Inspect the production Linux login separately; the Windows session does not
automatically synchronize credentials into WSL:

```powershell
$wkLinuxCli = '/home/warpkeep/.warpkeep/release-preparation-v1/toolchain/spacetime-2.6.1/spacetimedb-cli'
$wkLinuxConfig = '/home/warpkeep/.warpkeep/private/production-admin-v1/spacetime-cli.toml'
wsl.exe -d WarpkeepRunner -u warpkeep -- $wkLinuxCli --config-path $wkLinuxConfig login show
wsl.exe -d WarpkeepRunner -u warpkeep -- $wkLinuxCli --config-path $wkLinuxConfig list --server maincloud --yes
wsl.exe -d WarpkeepRunner -u warpkeep -- /usr/bin/stat -c '%F %h %u:%g %a' $wkLinuxConfig
```

The native config must be a nonsymlink regular file, single link, owner
`1000:1000`, mode `600`, with its parent private directory mode `700`. Check these
through the existing guarded caller as well; metadata does not establish provider
permission. The expected realm constants live in the
[existing-update adapter](../../scripts/ptr-production-existing-update-adapter.mjs)
and [G001 policy reader](../../scripts/genesis001-policy-observation-receipt.mjs).

Never add `--token` to `login show`. The listing and schema commands are marked
unstable by this CLI, so preserve the version with verification records.
Compare listed immutable identities to the release ledger before selecting a
database. Database names alone are not an adequate deployment target check.

On September 16, the owner's signed-in `ael-dev3` browser profile showed the
existing Warpkeep realms, while both CLI configurations selected a different
identity. Supported browser-backed login restored the correct publisher in
Windows and the Linux production configuration. Both then listed G001, G002 and
PTR with the identities pinned in the release source. This supersedes the empty
listings recorded since September 13; those listings did not mean the realms
were missing. No realm mutation was needed to repair access.

For a future mismatch, compare `login show` with the actual owner profile before
changing credentials. The supported `login --no-browser` flow clears the selected
old login and prints a temporary completion URL; open that URL in the verified
owner's browser session. Keep login URLs and credential bytes out of Git. Recheck
`login show` and the exact Maincloud database identities after completion. For
Linux, use the existing production config and restore its required `0600` mode
after login; see [the runner guide](0.4.0-linux-runner.md#existing-ptr-module-updates).

The verified schema read does not export rows. Player-data access and baseline
capture must use the approved authenticated operations and private evidence
store. Do not paste row exports, FIDs, raw receipts or configuration files into
public documentation or the workspace delivery package. If a row query returns
permission denied, stop that operation; successful schema/list access is not permission to
bypass row access restrictions.

## Mutation and recovery boundary

This guide intentionally supplies no standalone publish/reset commands. Use the
reviewed release operation entrypoints and exact frozen artifacts only after the
mandatory baseline, source, receipt, review and recovery checks pass.

Never reset or recreate G001. Freeze only new admissions while preserving existing
players and legitimate timers. Keep G002 sealed. Test the real owner journey in
PTR; do not open admissions to make a test pass. Recovery must preserve writes
made after deployment, not restore an old database snapshot over active state.

The final package must include the verified release-specific setup, deployment
and recovery entrypoints and their results. This access guide alone does not
satisfy those delivery requirements.
