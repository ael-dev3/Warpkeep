# 0.4.0 live delivery

## Current source checkpoint — 2026-09-10

The active Windows/GitHub branch contains documentation follow-up `f414b06e`
over functional implementation
`b6fe024ecf92ed68bfdc9003093b5a33bb932156`. Verify and CodeQL passed for the
functional head; replacement checks for the documentation-only follow-up are
running. This record describes delivery access and current observations; it
does not establish a shipped release.

Fresh read-only endpoint checks returned HTTP 200 for `https://warpkeep.com/`,
the auth bridge OpenID configuration, and its JWKS. The live frontend still
serves the existing Genesis 001 entry surface; no 0.4 frontend, Worker or
SpacetimeDB deployment is claimed by this observation.

The owner authorized completing and deploying 0.4.0 on 2026-09-04, including
necessary changes to the release architecture. Earlier instructions in planning
documents limiting work to local preparation or requiring the unavailable old
Mac are superseded for this delivery. Existing production data, authenticated
identity checks, and accurate deployment evidence still must be preserved.

## Verified access

- GitHub: the configured `gh` login has administrator/push access to
  `ael-dev3/Warpkeep`. The frontend uses **GitHub Pages**, with custom domain
  `warpkeep.com`; it is not a Cloudflare Pages project.
- Cloudflare: the configured Cloudflare connector can access the active
  `warpkeep.com` zone and `warpkeep-auth-bridge`, including its settings and
  deployment history. Its `auth.warpkeep.com` custom domain is attached to that
  Worker. The local Wrangler login currently selects a different account;
  an empty list there does not show that the production Worker is missing.
- SpacetimeDB: the configured 2.6.1 CLI lists the existing `warpkeep` database.
  Its immutable identity agrees with the auth bridge configuration. Genesis 002
  and PTR were initially absent and were created on 2026-09-04 UTC (September 5
  local time); see the new-database checkpoint below.
- Worker signing, session, admin, and RPC secrets are still configured. Reading
  their names does not expose their values. Preserve managed bindings when
  uploading a new Worker version; do not recover secrets into source or logs.

The initial access checks were authenticated read observations. Two new private
database modules have since been deployed. The complete 0.4.0 release, auth
bridge changes, PTR owner provisioning, and frontend activation are not shipped.

## Current repairs

- Restore missing SpacetimeDB standalone executable from the official 2.6.1
  Windows archive after checking the archive's published SHA-256 and matching
  the extracted executable to its archive entry.
- Regenerate G002/PTR bindings from their current module source. G002's obsolete
  generated admission/player calls must disappear because the sealed module
  no longer exports them. Generated bindings are not live database changes.
- Keep generated text at LF so Windows checkout conversion cannot break
  byte-for-byte catalog/binding validation.
- Fix the root TypeScript bootstrap declaration and public-key derivation test.
- Use standard `macos-14` for native-contract CI while preserving the ARM64
  assertion, pinned Node version, test list, and required status. GitHub's
  [runner reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners)
  lists this as a standard ARM64 runner for public repositories. The prior
  `macos-14-xlarge` job failed before starting because of billing.

## Remaining delivery work

1. Finish a clean frontend/module/bridge build and relevant regression checks.
2. Repair the release source inventory and its generated consumers. It contains
   obsolete paths and excludes source required by the approved PTR design.
3. Complete unfinished recovery execution. The fixture host now invokes fixed
   guest programs, but authenticated toolchain production, complete provenance,
   crash recovery, and filesystem/cache validation remain under repair.
   Synthetic unit tests do not prove an actual bootstrap or release is operational.
4. Build and test an executable release path using current authenticated
   accounts. Preserve Genesis 001, verify the newly created G002/PTR databases,
   their actual state, and preserve existing managed Worker secrets.
5. Publish reviewed source through the repository's checks and deploy the
   matching Worker/frontend. Verify live identity, version, auth, and realm
   behavior; record rollback versions and only then label 0.4.0 shipped.

Historical receipts cannot be reconstructed as though an old operation had
occurred. A replacement authorization flow must use fresh evidence and clearly
identify itself as a new recovery operation.

## Verification checkpoint — 2026-09-05

- The root production build passed, including TypeScript, asset integrity,
  production exclusions, the public-data boundary, and Farcaster manifest
  signature checks. This used Node 22.23.2; it is not a deployed build.
- G001, G002, and PTR typechecks passed after installing the existing pinned
  SpacetimeDB workspace dependencies with a frozen lockfile.
- PTR backend plus native-workflow configuration tests: 22 passed.
- Windows recovery bootstrap integration tests: 34 passed. Their per-test
  deadline allows the real Windows ACL subprocess checks to finish.
- Auth-bridge unit tests: 474 passed; recovery-service unit tests: 821 passed.
  Recovery service and Workerd test TypeScript checks also passed after a clean
  service-local frozen-lockfile install. The previous dependency junction was
  retained under `.git`; its external target was not changed or deleted.
- The recovery Workerd run stalled without reporting test results and was
  stopped. Its output included `RECOVERY_LEDGER_ARMING_ALREADY_USED` on
  shutdown. This runtime suite is **not verified**.
- A broad Windows root test diagnostic was stopped after exposing failures;
  there is no full-root-suite passing result. Release inventory derivation is
  being repaired with focused regression tests before another full run.

The owner's updated product direction favors the working gather/decision/
progression loop, clearer consequences, and delivery over feature breadth.
Economy rebalancing, broad renderer refactors, and unsupported claims of
physical-phone validation are not part of this verification checkpoint.

### Player-facing construction clarity

The catalogue and placement confirmation now show the quoted build duration
and the benefit at the quoted target level. Upgrades distinguish the currently
completed benefit from the proposed benefit. Placement confirmation also states
that confirmed placement is permanent and construction cannot be cancelled or
refunded. These changes do not alter costs, timers, production, or admission.

Screen/presentation regression suites passed 22 tests, and the root TypeScript
build passed. A real local browser check at a 390-by-844 viewport confirmed
readable project details and reachable warnings/confirmation controls in the
existing synthetic 2D QA fixture. This is not a physical-device, authenticated
backend, or WebGL performance result.

Five focused source-inventory regressions passed, covering unimported module
sources, the required PTR lockfile, private generated-table/build-output
rejection, and source-directory reparse rejection. The checked-in manifest and
runtime verifier have **not** yet been regenerated to match the new inventory;
the full release-closure verification remains outstanding.

The subsequent inventory repair makes the independently derived source set and
runtime verifier list agree at 1,022 members. It includes all G002/PTR module
source and the independent PTR lock, removes obsolete G002 admission bindings,
and includes the reached census/continuation/recovery bridge sources. The
manifest parser now enforces the same PTR path allowlist as file loading.
Combined inventory/parser regressions: 18 passed, one file-symlink case skipped
on Windows; the directory-reparse rejection still ran. The root TypeScript
build also passed. The old JSON digest manifest and workflow pins are still
unchanged: this is an inventory repair, **not** a completed refreeze or authority
to deploy changed protected source.

### New-database deployment checkpoint — 2026-09-04 UTC

The configured SpacetimeDB 2.6.1 CLI successfully **created** both databases on
Maincloud from the current module sources. Both commands used explicit module
paths, `--no-config`, `--delete-data=never`, and only the remote/skip-login
confirmation flags. Neither command targeted the existing `warpkeep` database.
No data deletion, G001 update, atlas import, owner provisioning, bridge upload,
or frontend activation was performed.

Module sources had no uncommitted differences from
`799814b5de3b3379c42961e86af595b7e5f82ac6`. Local published bundle SHA-256:

- G002: `06a2d29c17b82640dcfe80263301de1a6c1d5a50cc93a119e13b602f072d482e`
- PTR: `944be92c331f1a410950c64229c6c2b4d966f68a347a06ef7b1f0714d42d4cce`

| Realm | Database name | Immutable identity |
| --- | --- | --- |
| Genesis 002 | `warpkeep-genesis-002` | `c2003223f6e3c86e988775ddd458c3a45635d0d021e11131551471617c392194` |
| PTR | `warpkeep-ptr` | `c200df57bee179af512f05b3c7c328e3d4d7a6074ccc4ed976de84f94fb56d6e` |

Fresh pre-deployment verification: 63 targeted tests passed across
`genesis002SealedBackend`, `genesis002AdminBoundary`, and `ptrRealmBackend`.
Both module TypeScript checks and SpacetimeDB builds passed. PTR's build reported
that its package-local `tsc` executable was absent; its separate explicit
TypeScript check used the pinned parent workspace compiler and passed.

The live v10 schema shows 23 G002 tables and 24 PTR tables, all private.
G002 exports only the admin atlas operations and connection lifecycle; PTR
additionally exports its owner-management and owner-gated atlas reads.

The database metadata reports these **initial-program** hashes (not a claim
about a separate current-program endpoint or a signed release receipt):

- G002: `a41235b667dfe70467625e1fa009250e289243ae0eecf78b0da970add1338741`
- PTR: `67fc0fd0ee5bbccbfb99e4b103d82f983a6de8b08e941b5d4a441945eee1cd6e`

An attempted read-only population query through the configured publisher CLI
was rejected with HTTP 403 by both modules (`INVALID_GENESIS_002_ADMIN_SESSION`
and `INVALID_PTR_OWNER_SESSION`). Publisher credentials do not satisfy the
application's admin JWT contract. No population count was returned, and no
attempt was made to bypass that authorization check. A valid Warpkeep admin
session is required for the next protected verification/provisioning steps.
PTR's sole owner FID also still needs to be established, not inferred.

The separate legacy backend pure-test diagnostic finished with 498 passes and
32 failures out of 530 tests. That suite is not green and was not treated as
approval to modify Genesis 001. Full release verification remains incomplete.

Follow-up investigation found Windows CRLF source-check failures, not missing
admission functions. Preserving repository LF for backend source alone improved
the full diagnostic to 525 passes and five failures. The remaining cases were
traced to CRLF in migration fixtures/tooling/browser source and a Windows
separator mismatch in the stateful harness's bundle-input assertion. Formatting
and harness corrections are under verification; no production backend behavior
was changed or republished. The initial claim that these failures indicated
removed G001 admission functions was not supported by the source inspection.

PTR's package-local dependencies were subsequently materialized from its frozen
lock. pnpm returned `ERR_PNPM_IGNORED_BUILDS` for esbuild's unapproved lifecycle
script; it was not approved or run. The installed package-local TypeScript
check and SpacetimeDB build nevertheless passed without the earlier missing-tsc
warning. This local rebuild is not a second deployment and is not the bundle
whose published SHA-256 is recorded above.

The standalone PTR install was then moved, together with pnpm's generated
workspace approval file, to `.git/ptr-standalone-install-20260904` because the
audited payload/CI contract requires PTR to use the parent workspace. Nothing
was deleted and no lifecycle approval was granted. With that layout restored,
all 63 targeted G002/PTR tests passed again and the rebuilt PTR bundle matched
the published SHA-256 above exactly.

The complementary backend run (all pure-test files except the four in the
already-running targeted run) passed 512 tests with no skips or failures.
The targeted run subsequently completed: 86 tests passed, no skips or failures,
in 808.6 seconds. Together these non-overlapping runs cover 598 backend tests.
The Windows source-format and exact bundle-input assertion corrections are
verified for this batch. This does not establish full-root or Workerd success,
nor authorize a Genesis 001 backend update.

Admin-access clarification: the source implements service-token endpoints
authenticated with the bridge's configured `ADMIN_TOKEN_SECRET`. A dashboard
login or ordinary player session is not sufficient. The existing secret must
be configured privately for the authorized operator, or an explicit recovery/
rotation must be performed; do not ask for a secret to be pasted into chat.
The deployed bridge must also support the required realm-specific token route
and configuration before these new databases can be administered.

### Delivery scope after the product assessment

Treat the assessment as product direction, not an authenticated playtest. The
0.4 delivery priority is the existing sealed realm split, reliable entry and
reconnection, and clear gathering/construction decisions. Preserve Genesis 001
at 0.3 with its existing players; do not open Genesis 002 or admit a test cohort.
PTR remains a separate, explicitly provisioned admin-only realm.

The construction clarity changes above are the bounded player-facing work for
this release. New production bonuses, shortened construction timers, and
additional building mechanics need a separate PTR experiment before promotion;
they are not prerequisites for shipping the sealed release. Do not expand this
release into a renderer/provider rewrite. Extract modules only when needed to
fix a concrete release defect behind existing tested interfaces.

Physical-device acceptance remains outstanding: authenticated entry, dispatch,
reconnect, background/resume, and repeated scene switching need device evidence.
Construction can be exercised only where it is actually enabled. Record load
time, frame pacing, memory growth, and failures on named devices; synthetic
browser fixtures and passing source tests must not be reported as phone proof.

### Windows sealed-launch checkout and player-flow verification

The source-authority gate first failed because Windows checkout had converted
its pinned source and declaration from LF to CRLF. Both committed blobs and the
LF-restored files matched the existing SHA-256 pins; no pin was changed. Source
and configuration text now has explicit LF checkout attributes. Three new tests
failed before that correction and passed afterward, including real Git checkout
filter checks with `core.autocrlf=true`. Eighty-six verifier inputs were restored
to their exact committed bytes, with no logical source edits.

Fresh focused verification passed 182 tests across ten files covering direct
entry, reconnect, PTR isolation/connections, Inner Keep screens/provider,
navigation, renderer recovery/cleanup, and checkout bytes. The root TypeScript
build also passed. These are local automated results, not authenticated live or
physical-device verification.

The separate sealed-launch verifier suite remains red: 92 passed, 61 failed.
The command-line gate now reaches
`SEALED_LAUNCH_G001_ADMISSION_MONITOR_CURRENT_STATE_INVALID`. Its current source
hash is `50776faaeb1ccd0c7357e6058ed90ac5a4ad5ad043444126089bdd45d1fd4560`,
while the verifier pins
`10c8286a38ac81a5672280dcede60f712a95bc78af2f263e3ee8cc40d4afd5ac`.
That is a real stale source commitment after the earlier continuation changes,
not another checkout conversion. The suite also invokes `/usr/bin/git`, which
does not exist on this Windows host. Neither failure is waived, and no protected
pin is updated merely to accept the present source.

### Production dependency audit — 2026-09-05

- `pnpm --dir services/auth-bridge audit --prod --json`: exit 0, no advisories.
- `pnpm --dir services/release-recovery audit --prod --json`: exit 0, no advisories.
- `npm audit --omit=dev --json`: exit 1, two moderate entries (`stream-json` and
  its dependent `jayson`), no high or critical production entries. This is not
  a clean dependency audit or a full development-dependency audit.

The root lock resolves Mini App SDK/core -> Solana web3 -> `jayson@4.3.0` ->
`stream-json@1.9.1`. The current Jayson package still declares `^1.9.1`.
The [maintainer advisory](https://github.com/uhop/stream-json/security/advisories/GHSA-528h-pc64-c93x)
describes a nesting-depth denial of service in the path filters and identifies
3.5.0 as the patched version; it explicitly excludes the streamers from this
specific issue. Installed Jayson's Node utilities use `StreamValues` and
`Verifier`; its browser client uses native JSON parsing instead.

A fresh in-memory Vite production build (`write: false`) inspected the actual
emitted chunk module inventory: 29 chunks, 898 modules, zero `stream-json`
modules, and only Jayson's request generator/browser client. This supports
non-reachability in this browser build, not a claim that every tool/runtime is
unaffected. The build exited 0 with a CSS-plugin timing warning. No package or
lockfile was changed, no dependency override or advisory suppression was added,
and the repository audit gate remains outstanding.

Follow-up full audits (including development dependencies) returned no
advisories for auth bridge or recovery and the same two moderate root entries;
no additional development-dependency findings were reported.

License-cutover, tracked file-size, and Greater Realm public-boundary checks
passed. The license check initially rejected four stale manifest index entries
after LF restoration. Each file was byte-compared with its HEAD blob before an
index-only renormalization, and the staged diff was verified empty. No license
text, package contents, or policy assertion changed. The public-boundary check
reported 2,427 tracked paths and 1,550 scanned entries.

### Fresh recovery Workerd verification — 2026-09-05

Using the pinned Node 22.23.2 harness and the installed service dependencies:

- V2-only runtime suite: 12/12 passed, exit 0, 93.07 seconds.
- Complete V1 + V2 runtime suite: 36/36 passed across both files, exit 0,
  80.66 seconds. Invocation: `node node_modules/vitest/vitest.mjs run
  --config vitest.workerd.config.ts --reporter=verbose` from the recovery service.

No runtime code, cleanup hook, timeout, retry, or assertion changed for these
runs. The complete run includes the previously implicated reverse-role and
stale-alarm tests. This establishes a fresh full-suite pass, not a demonstrated
fix or root cause for the earlier intermittent global-reset timeout. Task 7
fixture materialization, signer composition, and the release gate remain
unfinished; no live activation is implied.

The Windows Git failure also exists in the verifier's two production Git
adapters, not only in its test fixture helper. Both fix `/usr/bin/git` and a
Unix-only clean environment. A portability correction must preserve fixed
executable selection, configuration isolation, disabled checkout caches,
bounded output, and exact history/tree checks. Merely changing the test helper
to PATH lookup would not repair this boundary.

### PR-failure replay and remaining workflow checkout bytes

Fresh GitHub CLI readback still shows only PR 228, at `2d7e24f76`, with Linux,
native-contract, and aggregate Verify failing. Its Linux log reports six failed
tests and a failed PTR closure suite. These remote results predate the local
repairs; nothing has been pushed or merged in this checkpoint.

A local replay of the four implicated files completed with 34 passed, two
failed, and 13 skipped (146.70 seconds). The PTR closure tests now pass,
including source inclusion, independent lock, private-table/build-output
rejection, directory-reparse rejection, and repository-escape rejection; the
file-symlink case remains skipped on Windows. PTR generated binding verification
also passes. The two failures were:

- B0 workflow text still checked out with CRLF, causing an earlier bootstrap
  rejection instead of reaching the documented stale-workflow-pin boundary.
- B0 closure test still asserting 997 members although the derived and runtime
  member lists agree at 1,022. This is an outstanding generated-consumer refreeze,
  not a missing-source-list failure.

Both notification workflow files were restored from CRLF to LF only after an
all-target preflight proved their normalized bytes exactly equal HEAD. Git diff
for both files remained empty; no source or pin changed. The previously failing
bootstrap test then passed, followed by a complete B0-workflow/PTR-backend rerun:
26 passed, 12 platform-specific skips, exit 0 (3.65 seconds). Passing the B0
structural test still means it reaches and rejects the stale canonical workflow
pin, not that the workflow is ready to deploy. The 997-member assertion remains
red until the generated closure transaction updates its consumers together.

### Full-root regression and recovery review — 2026-09-05

The complete Windows root run at `f6abc6c` finished under Node 22.23.2:
5,515 passed, 690 failed, 149 skipped; 91 failed files, 395 passed files,
one skipped file, and four unhandled errors. Duration: 1,013.21 seconds.
This is a failed release check, not approval to deploy.

The report includes absent Unix Git/shell executables, unsupported POSIX
owner/mode and process assumptions, symlink-fixture permission errors, stale
release commitments, and source-text checkout drift. These categories are not
a waiver: remaining assertions and release failures need individual diagnosis.

After the run, 1,609 files outside the recovery service were restored from
CRLF to their exact committed LF bytes, following all-target canonical-path,
non-link, UTF-8, and normalized Git-blob equality checks. The CC BY legal-code
file was separately restored to its exact committed bytes and existing hash.
No logical source, license text, or pinned hash changed. CSS, TOML, `.gitignore`,
and the license file still need durable checkout-attribute coverage.

Post-format results:

- Six CSS/scene/browser-probe/repository checks: 96 passed, two skipped.
- Activation-security/local-QA/license batch: 244 passed, two failed, six skipped.
  Both remaining failures are in the local QA test's dependency scanner: its
  hard-coded slash prefix stops traversal on Windows. Do not remove the
  transitive-boundary assertions; repair the scanner and verify the real graph.

The independent recovery-fixture review found six important issues: missing
authenticated bootstrap production, incomplete toolchain evidence schema,
namespace response framing, interrupted-output recovery, filesystem containment,
and complete offline-cache verification. It also found early-failure buffer
cleanup and response-stream bounding defects. The original implementer is
addressing these with disposable tests. No fixture materialization ran, so the
interrupted-output defect has not destroyed generated or production data.

The producer must distinguish immutable source policy from generated proof.
Missing verified public-artifact commitments remain missing, not guessed or
replaced with synthetic test values. Task 7 and the later signer composition
remain incomplete. No complete 0.4.0 deployment occurred in this checkpoint.

### Product-direction regression check — 2026-09-05

A fresh Node 22.23.2 run of `innerKeepScreen`, `realmNodeWorkerDispatch`,
`RealmChoiceSelector`, and `realmChoiceMenuIntegration` passed all 51 tests
across four files (19.66 seconds, one worker). This checks the existing
construction details, dispatch controls, and realm-selection behavior; it is
not an authenticated end-to-end playtest or evidence of physical-phone speed.
React reported asynchronous `act` warnings in the realm-menu integration tests.

The product scope remains construction clarity and dependable existing flows.
No timers, building bonuses, admissions, or production data were changed in
this check. The failed full-root release gate and unfinished recovery work above
remain open; these focused passes do not authorize calling 0.4.0 shipped.

### Remote sync and toolchain evidence — 2026-09-05

Fetched `upstream/main` and `upstream/codex/prepared-keep-bindings-fix` without
merging or changing the working files. Main remains `ce1b810`; the only open
PR is #228, "Prepare sealed 0.4.0 realm launch", at `2d7e24f`. The local
`f692fd4` history contains both remote tips and is 134 commits ahead of main.
PR Verify remains failed; fetching is not publishing or merging the local work.

The official SpacetimeDB 2.6.1 Linux archive was downloaded into a unique
ignored `.git` audit directory. Its actual 57,464,969 bytes and SHA-256 match
the approved pin. Both regular executable members were streamed into bounded
hash checks, without filesystem extraction or execution. Their verified member
identities were provided to the recovery implementer. Node signed-checksum and
signature verification remains outstanding; no production toolchain readiness
or materialization is inferred from this partial artifact evidence.

Follow-up: direct bounded official HTTPS downloads supplied the exact Node
24.19.0 and 22.22.3 checksum/signature files. Their signatures verified with
the exact pinned EdDSA/RSA public keys using isolated Windows GPGv keyrings.
Both Linux x64 `.tar.xz` archives match their signed checksums, and the regular
0755 `bin/node` members have measured byte counts and SHA-256 hashes. Those
commitments were handed to the implementer. No downloaded executable was run.
This is independent public-artifact evidence, not the pinned Ubuntu bootstrap
verification or complete production readiness. The two disposable public-key
audit keyrings remain because cleanup was rejected; no private keys are present.

### PTR administration configuration readback — 2026-09-05

Authenticated Cloudflare settings GET returned HTTP 200 for the correct account
and `warpkeep-auth-bridge`. `SPACETIMEDB_DATABASE` still names the existing G001
identity. `PLAYER_CANARY_OWNER_FID`, `PTR_ENABLED`,
`PTR_SPACETIMEDB_DATABASE`, and `PTR_OIDC_AUDIENCE` are absent.
`ADMIN_TOKEN_SECRET` exists as a managed secret binding; no secret value was
read or returned. No settings were changed.

The code requires a canonical owner FID and a fresh enabled G001 admission
with a positive auth epoch before issuing a PTR provisioning token. A publisher
login or Cloudflare login is not a substitute. The owner's numeric FID remains
a required operator input; do not infer it from unrelated service accounts or
open a new G001 admission to satisfy this gate.

Fresh bridge `test/ptrOwnerExchange.test.ts`: 20/20 passed, exit 0, 18.64 seconds.
This covers owner authorization, disjoint audience, disabled/malformed rejection,
private response behavior, and server-only admin routes using test fixtures.
It does not establish authenticated live PTR provisioning.

### Exact-version local Pages build — 2026-09-05

Installed Node 22.22.3 and bundled npm 10.9.8 only under
`.git/ci-node-22.22.3`. The Windows Node executable and ZIP both match entries
in the independently signature-verified Node SHASUMS file. The npm subtree
was validated for bounded regular files/directories, canonical paths, and
duplicates before no-clobber extraction. No global toolchain or dependency
installation was changed.

With that Node first on PATH and the exact bundled npm CLI, `npm run build`
completed exit 0 under `GITHUB_PAGES=true` and `DEPLOY_BASE=/` (session 7346).
TypeScript, source/runtime assets, production assets/exclusions, the public-data
boundary, and Farcaster manifest/signature checks passed. Vite transformed
1,992 modules and retained its warning for chunks larger than 600 kB; no budget
was raised or warning suppressed.

The preceding build also passed, but its inherited npm launcher was 12.0.2;
the completed 7346 rerun is the exact Node/npm-version result. This Windows
build does not reproduce Linux ownership/process tests. The package is still
the prepared `0.3.43` state pending the exact activation change; it is not a
shipped or activated 0.4.0 release.

### Required release checks and GitHub protection — 2026-09-05

Authenticated GitHub API readback confirms `main` is protected. Strict required
checks are `verify`, `auth-bridge`, `spacetimedb-module`, `analyze`, and `CodeQL`.
These apply to admins too; linear history is required and force pushes are
disabled. The `github-pages` environment permits only the `main` branch. Its
returned rules have no required-reviewer or wait-timer gate. No protection or
environment setting was changed.

The current Verify workflow has no recovery-service job, and root Vitest does
not discover that service's tests. Add the full recovery check as a dependency
of the required aggregate Verify before final source refreeze. Local unit or
Workerd passes do not replace this missing hosted coverage. No branch-protection
weakening is needed to make the new check required through that aggregate.

The exact Node 22.22.3, single-worker replay of `innerKeepOperator`,
`pagesDeployConfig`, and `licensePolicy` finished with 66 passes and seven
failures (164.40 seconds; unchanged deadlines). Two failures assert Unix-only
open flags that are undefined on Windows after the expected rejection already
occurred; one invokes absent `/usr/bin/git`; two require a newline path or
symlink fixture unsupported by this checkout; two valid Git cutover fixtures
exceed their existing ten-second deadlines. No assertion, timeout, runtime
security check, or release pin was relaxed. These focused diagnostics are not
a green full-root result.

### Native Linux replay of Windows failures — 2026-09-05

Created a disposable Ubuntu 24.04 checkout detached at `74f38a5`, with clean
Git/npm configuration, scratch-local dependencies, and no copied provider
credentials. Node 22.22.3/npm 10.9.8 came from the previously verified Linux
archive; the staged private Node executable's SHA-256 matches its measured
signed-archive member. Root, bridge, and parent SpacetimeDB dependencies were
installed from existing locks with lifecycle scripts disabled. The original
Windows worktree and active recovery implementation were not modified.

The same `innerKeepOperator`, `pagesDeployConfig`, and `licensePolicy` suites
now pass **all 73 tests across three files** on Linux (7.30 seconds), without
changing assertions or deadlines. This provides native coverage for the seven
Windows failures above; it does not repair the Windows harness or prove the
whole release. The first Linux attempt had only root dependencies: its license
tests passed, but two suites could not collect until bridge-local TypeScript
and YAML were installed from the bridge lockfile.

The main root replay finished with the CI workflow's two-worker split:
6,180 passed, 74 failed, and 16 skipped across 485 files (nine failed files),
in 916.38 seconds. The two separate serial closure suites are now running.
No full-root passing result, recovery materialization, or live deployment is
claimed.

The failures include stale release commitments and file-list consumers, the
unfinished Task 6E authority factory, the outdated G002 label assertion, and
four toolchain/harness cases. Two toolchain failures reject esbuild's script
wrapper instead of its native binary after lifecycle scripts were disabled;
the workflow's normal root install enables them. Two B0 fixtures have an
ancestor-boundary replacement pattern that does not match the current shell
function syntax, leaving the scratch checkout's writable `/tmp` ancestor in
their validation path. These diagnoses require focused verification; neither
the executable comparison nor production filesystem protections were relaxed.

The serial closure replay was subsequently stopped after its worker accumulated
over 22,000 unreaped `tsc` children. Its processes exited; no final JSON report
or completed test counts were produced. A bounded twelve-parse diagnostic
reproduced twelve zombie children despite calling dispose/close, then zero
after yielding to the event loop. The source checker creates one native
TypeScript API process per file inside a synchronous graph traversal. This
resource-lifecycle issue needs a targeted repair before repeating the large
closure run; the aborted run is not a passing release check.

After the aborted run exited, the normal pinned esbuild installation step was
run offline in the disposable Linux checkout. The complete toolchain-security
suite then passed 15/15 unchanged tests (920 ms). This clears the two earlier
esbuild setup failures without relaxing executable integrity checks; it does
not clear the separate parser lifecycle issue or the failed full-root gate.

### Recovery bootstrap review — 2026-09-05

Task 7 source correction f9de7e9 reports 80 focused and 852 service unit tests
passing, but independent scoped review did not approve it. Six original
findings were addressed; missing producer-boundary verification and output
durability remain open, with four further Important defects: receipt digest
semantics, invalid isolated Git-reader structure, mismatched G002 workspace
layout, and unanchored endorsement of pre-existing cache state.

The original implementer now owns fix round 2. Historical receipt commitments
must remain distinct from Linux source/cache commitments; G002 must retain its
authenticated Spacetime workspace layout; an adjacent manifest/catalog cannot
establish new cache trust by agreeing with itself. Real disposable Git and
isolated signature tests are required where success mocks missed these defects.
No protected receipt, source pin, validation deadline or security requirement
was waived. The real operator bootstrap/write/check gate remains unexecuted,
the signer has not been composed, and no live 0.4 deployment is claimed.

### PR execution and B0 diagnostic follow-up — 2026-09-05

Authenticated GitHub inspection still finds only PR 228, head 2d7e24f, with
protected main at ce1b810. Its latest Verify run is 33455545465 (September 1).
The native-contract job 99694617321 has no executed steps: its annotation says
GitHub refused to start it because of failed account payments or a spending
limit. This is historical job evidence, not a current account-billing readback;
the owner should confirm account readiness before the required check is rerun.
No billing settings, branch protections, or required checks were changed.
The Linux job separately ran and failed tests; clearing the account issue
alone would not make the PR ready to merge.

The B0 positive-replay failure was independently reproduced in the disposable
Linux checkout at 74f38a5 with exact Node 22.22.3 and one Vitest worker.
The test helper searches for `function verify_immutable_executable_path() {`,
but the workflow omits `function`. The intended fixture-root restriction is
therefore never inserted, so validation reaches `/tmp` (mode 1777) and rejects.
A temporary one-line test-helper correction changed the same focused case
from failure to success; the complete `authBridgeNotificationB0Workflow` file
then passed 24/24 tests with no skips and unchanged assertions/deadlines.
The scratch change was reverted. This confirms the cause and supports a
test-only repair with explicit transformation-match coverage; it is not a
retained release-branch fix or a successful full-release verification.

### Local recovery setup and diagnostic verification — 2026-09-05

The Windows bootstrap diagnostic completed with exact Node 22.22.3 and one
worker: 34/34 tests passed, no skips, exit 0, in 240.53 seconds. The only
temporary test change added `--no-checkout` to the separately registered Git
worktree fixture; assertions and deadlines were unchanged. That change was
reverted and the test file has no semantic diff. The preceding unchanged run
had 32 passes and two failures with uninformative `STACK_TRACE_ERROR` messages;
elapsed time alone does not establish their cause. This diagnostic is not a
passing full-root release gate or a retained test repair.

The approved local bootstrap generator then completed successfully against the
fixed private recovery root. It created the missing disabled marker, RPC secret
and census pepper, preserving the existing signing key and its public
thumbprint. A second invocation returned `existing`, verifying idempotent reuse
without replacement. Secret values were not printed, committed or uploaded.
The owner FID, auth-bridge public-key input, authenticated realm receipts and
toolchain attestation remain outstanding. No provider or activation mutation
was performed and this does not satisfy the full bootstrap verification gate.

Independent review of Task 7 correction 41c8b11 cleared the four digest,
isolated-Git, G002-layout and existing-cache provenance defects. It still
requires a combined test of the real producer authority chain and a durable
namespace barrier when retrying rollback after a failed directory flush. It
also identified a new Linux test-launcher defect caused by unconditional
Windows WSL execution. The original implementer owns focused fix round 3;
Task 7 is not approved and the downstream signer task remains undispatched.

A subsequent authenticated Cloudflare readback returned 200 for the live
bridge's settings and deployments, with its existing August 28 deployment
unchanged. Its configured issuer and key ID matched bounded, direct HTTPS
OIDC discovery/JWKS readback. The missing bridge public-key input has now been
provisioned locally as canonical JSON with verified owner-only protection;
it imports as a valid P-256 public key and differs from the recovery key.
The bridge's private key was not extracted and no key was rotated. Nine
required inputs remain missing: owner FID, toolchain attestation, and seven
authenticated realm receipts. Local input preparation does not complete
the full verification/materialization gate or change live service state.

### Source-review closure and retained B0 repair — 2026-09-05

Task 7 correction f8e48b0 passed its fresh independent scoped review: all three
remaining findings are addressed, with no new issue. Its seven-file covering
run passed 93 tests; bootstrap tests passed on Windows and native non-root
Linux. The latter uses an isolated user namespace for ownership-sensitive
fixture children and requires that host capability. The combined test uses
real authority functions with simulated transport/process leaves, supported
by separate real Git and signature tests; it is not an actual operator run.
Task 7's real write/immediate-check gate remains incomplete, and the signer
task is still undispatched under its prerequisite rule.

The separately approved B0 harness repair is now retained in 3214039 and
independently reviewed clean. It fixes the incorrect function-header match
and rejects missing/ambiguous fixture transformations. Eight added regression
cases failed before the repair and passed after it. Full Linux verification
passed 32/32; Windows passed 20 with its 12 unchanged POSIX-only skips. The
verified Linux scratch copy was reverted after review; the main worktree keeps
the committed repair. Production workflow/scripts, assertions and deadlines
were unchanged. The whole-root release gate remains unproven.

The Windows bootstrap fixture optimization is retained in 4506d02 and also
independently reviewed clean. It registers a real linked worktree without
checking out all tracked files; the same private-root rejection and cleanup
remain exercised. Fresh full verification passed 34/34 without skips or
deadline changes (220.74 seconds overall; linked-worktree case 475 ms). This
does not establish that all earlier timing failures have one cause or repair
the remaining full-root, materialization, CI and live verification gates.

### Compiler lifecycle repair — 2026-09-05

The owner approved the bounded compiler repair. Closure traversal now owns one
native TypeScript API per graph, not per source file. Each source snapshot is
disposed before the next file; the previous virtual file is removed and its
closure is reported to the compiler. The graph closes its API even when source,
import, path, snapshot creation or disposal fails. No compiler survives into a
later graph scan, and returned source text must match the current input bytes.
No manifest, source pin, workflow, import allowlist or deadline was relaxed.

All eight initial lifecycle regression cases failed against the old implementation
and passed with the repair. Independent review then identified primary-error
masking when validation and cleanup fail together. Four additional regression
cases failed before that correction and passed afterward; the original policy
error now survives cleanup failures, while cleanup-only errors still fail the
scan. The corrected source passed scoped re-review without remaining findings.
Combined with the existing PTR closure suite, exact Node 22.22.3 verification
passed 20/20 on Linux and 19 tests on Windows with its
one existing file-symlink skip. The Linux test observes real `/proc` children:
at most three new compiler children for the full three-graph derivation before
yielding, then zero after the event loop reaps exits. This is a per-derivation
bound, not synchronous reaping or a global bound across unlimited synchronous
calls. Root app/test TypeScript checking also passed with build info in `.git`.
The previously aborted large serial suites completed on the initial repair:
96 passed and 16 failed out of 112 in 148.92 seconds, with no remaining worker
or compiler processes. Failures include stale source-verifier hashes, obsolete
997-member expectations versus the derived 1,022 members, manifest/workflow
policy checks, and the positive Node-launch harness. The final corrected source
completed the same suites in 145.71 seconds with the same 96 passes and 16 failed
cases, no skips, and no remaining compiler processes. No full-root passing result
is claimed and nothing has been deployed by this repair.

### Required recovery CI coverage — 2026-09-05

The previously missing recovery-service job is now a required predecessor of
aggregate Verify. It uses the existing pinned checkout/setup actions, exact
Node 22.22.3 and pnpm 11.7.0, a frozen service-only install, the complete service
`check`, and dependency audit. It has no deployment environment, secrets,
extra permissions, conditional bypass or provider operations. The aggregate
rejects failure, cancellation and skipping of any predecessor; semantic tests
exercise all 1,024 combinations of its actual result predicates.

Two Linux failures were Windows-specific test assumptions, repaired only in
tests. Windows path/platform emulation delegates public source bytes and file
descriptors to the real checkout, retaining source/Git capability checks and
all 19 fixed WSL invocation assertions. Private-record tests execute actual
ACL validation with synthetic process responses and verify buffer zeroization.
A separate native-host test proves unsupported Linux source-host rejection
before any WSL process. Numeric Windows file-version checks now run everywhere,
replacing a test that returned early outside the exact Windows installation.
Production authorization, source policies and fixture programs are unchanged.

Final full checks passed on Windows and a fresh non-root native Linux checkout:
both TypeScript configurations, Wrangler type consistency, 865 unit tests,
and 36 Workerd tests on each platform, all exit 0. The clean Linux checkout
contained only the recovery service's dependencies; root, bridge and Spacetime
dependencies were absent. All three changed service test/helper files matched
the Windows source by SHA-256. The 26 workflow/security tests and root app/test
TypeScript check also passed. Recovery audit found no known vulnerabilities.
Independent scoped review found no remaining issue.

Hosted CI uses Ubuntu 22.04 to avoid Ubuntu 24.04's AppArmor restriction on
unprivileged user-namespace capabilities used by disposable ownership fixtures.
No elevated fallback or global policy change was added. Local Linux results
come from WSL Ubuntu 24.04 with working namespaces, not the hosted runner;
the hosted result remains unproven. The broader root tests, stale release pins,
real recovery materialization/activation gates and live release remain open.

### Prepared-launch fixture repair — 2026-09-05

The remaining positive prepared-launch fixture failed before its intended
assertions because it traversed the installed Node path through writable `/tmp`.
Its Linux-only host adaptation now uses a real copied Node with mode 0555 inside
the canonical private fixture, requires that direct-child relation, and stops
ancestry traversal only after validating the private root. Missing/duplicate
function or stop locations reject the transformation. Production workflow,
Node attestation, permissions, pins and deadlines remain unchanged.

Stronger negative assertions exposed three previously masked tests: entrypoint
mutation, manifest authority mismatch and child credential separation had been
passing on unrelated Node rejection. They now reach their intended boundaries.
The entrypoint-mutation check uses the existing synthetic closure verifier; it
is not a substitute for real closure verification. The positive synthetic module
now requires secrets to be absent during import, checks them at entrypoint
invocation, and returns the required verified read-only recovery outcome.

All four initial focused cases failed at the unrelated Node guard before repair.
Final Linux focused verification passed 19/19. Removing the new fixture guards
made all nine guard tests fail as expected; restoring them returned the same
source bytes. Windows passed the nine pure fixture guards, and root app/test
TypeScript passed. A broader Windows selection still has eight POSIX-process/
symlink harness failures; this repair does not claim full Windows or Darwin
execution coverage. No new skip was added. Independent scoped review is clean.

Both affected serial suites completed on native Linux in 161.86 seconds:
106 passed, 15 failed, zero skipped out of 121. Compared with the earlier 16
failures, only the positive launch failure is removed; no new failed case was
introduced. Remaining failures are stale generated pins/counts, manifest and
workflow policy checks. The full branch is not verified or deployed.

Fresh authenticated GitHub readback still shows only open PR #228 at 2d7e24f,
blocked, with its September 1 Verify run failed. That historical run's native
job annotation reports account payment/spending-limit rejection; it is not
evidence of the current billing state or a new hosted run.

### Binding-tree verifier repair — 2026-09-05

Commit `d9fdf49` replaces the generated-binding verifier's permissive file
walker, which silently ignored links and special files. The shared write-free
reader now rejects invalid paths, links, foreign output and case collisions;
requires root `index.ts`; bounds depth, file count and bytes; and compares
sorted paths without normalizing content. Descriptor reads are bounded before
allocation, including when a file grows after its initial size check.

The focused suite passed 19 tests on Windows with four explicit POSIX-only
skips, and all 23 tests on native Linux with no skips. All four task files
matched across the tested checkouts by SHA-256. App/test TypeScript passed;
read-only validation accepted all 237 existing G001/G002/PTR binding files.
Independent scoped review found no issues. No generated bindings, release
pins/counts/manifests, workflow, dependencies or live data were changed.

The complete fixed generator adapters are still pending. Existing CLI binary
attestation and publisher `--no-config --js-path` generation can be reused.
Official SpacetimeDB 2.6.1 source confirms `--module-path` can execute a local
compiler and write module build output, so staging only the binding destination
is insufficient for write-free derivation. The prepared-release trusted-host
contract, final refreeze, remaining recovery integration and live deployment
remain open; this repair is not a release-readiness claim.

### Independent PTR source-build repair — 2026-09-05

The real PTR publisher now prepares its source build through PTR's independent
package and lockfile, rather than the G001/G002 workspace dependency closure.
The new helper verifies the complete selected Darwin ARM64 dependency graph,
offline archive integrity and package identity; installs only beneath PTR;
binds dependency provenance; and verifies source/dependency state around the
synchronous build callback. Its only permitted generated file is PTR's bundle.
The frozen G001 projection, generated bindings and release pins are unchanged.

Initial implementation `58a7b32` passed 46 Linux checks, but independent review
found incomplete platform-metadata validation, a cleanup error that could hide
the original failure, and missing real-writer test coverage. Fixes `2987a05`
and `a4f6dd8` address all three. Scoped re-review found no remaining important
issues. The minor suggestion for additional size-limit boundary tests remains
recorded for final branch review.

Final verification used exact committed file copies on Linux: 56 passed,
three existing Darwin-only tests skipped, zero failed. This includes the real
descriptor-relative Python writer, native package/bin symlinks, containment,
provenance and cleanup. Windows passed all 34 helper tests and 17 publisher
tests; native POSIX mode/link behavior is not established by Windows mocks.
App/test TypeScript passed. No actual module build, binding generation,
dependency download, credential access or deployment was performed by this
repair.

The remaining fixed binding runtime/adapters, refreezer and recovery/activation
integration are still open. The former Darwin-only delivery requirement has
been superseded by the owner's full-local production direction below. Fixture
success alone does not establish release readiness, and 0.4.0 is not yet live.

### Full-local production and reviewed Linux PTR build — 2026-09-05

The owner has removed the Mac runner from scope. Windows/WSL must provide the
full preparation, verification, deployment, recovery and activation workflow
against the existing remote hosts. Design amendment `d5caec3` records this
scope; no Mac availability check or Mac execution is a delivery prerequisite.
Existing G001 data/latest 0.3 behavior and closed admissions remain protected;
G002 stays sealed and PTR remains owner-only.

Implementation `115b9c3` factors the PTR lifecycle into one shared core and
two fixed wrappers. The existing Darwin contract is preserved for compatibility;
the new Linux x64 wrapper has an exact 15-package closure and its own provenance
domain. There is no caller-selected platform or root-lock fallback. Independent
review passed both spec compliance and code quality with no findings.

The exact six committed source/test blobs were copied and hash-verified before
native Linux execution: 84 passed, three existing Darwin-only skips, zero
failures across six test files. Real staged-writer tests cover both retained
Darwin layout and Linux layout/provenance across two fresh roots. Typecheck
passed. This completes the dependency-build component, not actual generation,
refreeze or deployment.

Production migration must separate unprivileged disposable local verification
from trusted production execution. Existing PR workflows execute checkout code
without a trust gate; runner labels and environment approvals are not isolation.
No new runner has been registered, no production credentials transferred, and
no live mutation performed by this migration work. Fixed runtime integration,
local production toolchain/identity, receipt continuity, final release checks,
deployment and PR closure remain outstanding.
