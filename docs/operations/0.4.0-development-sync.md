# 0.4.0 development synchronization

Established 2026-09-06 with owner authorization. GitHub source publication is separate from production deployment.

- Publish completed, reviewed task commits to `codex/prepared-keep-bindings-fix` and keep PR #228 as a draft while release work remains.
- Push explicit commit-to-branch refspecs, without force. Verify the remote SHA after each push. If the remote has advanced independently, fetch and reconcile; never overwrite it.
- Keep local source plans, tests, implementation, and public evidence under version control. Exclude credentials, private receipts/player data, caches, dependency directories, disposable builds, and machine-specific operational state.
- Review substantive uncommitted changes before staging. Do not stage the entire dirty checkout or normalize unrelated files merely to make status empty.
- Scan outgoing history for secrets. Review findings individually; synthetic test material and public-key hashes must not be represented as a clean scanner result without documented narrow exceptions.
- A push, draft PR, or passing CI does not mean the release is live. Main integration and production deployment remain governed by the release checklist.

The initial synchronization published 259 commits through `de801391d26cf3bc1c3a1b80b0342a20181ae8d5`; the remote branch SHA was verified equal. PR #228 was converted to draft. Main and production were not changed.

The pinned Gitleaks 8.30.1 scan of the outgoing range reported 14 findings. Inspection identified Node public signing-key hashes, a public recovery-key thumbprint, schema field names, and synthetic test credentials/invalid-key/JWS fixtures. No production credential was identified among those findings. Scanner exceptions and successful CI remain separate unfinished verification work.

At that synchronization, `Verify` and `CodeQL` were triggered for the PR. Existing deployment configuration limits automatic Pages deployment to successful Verify runs from a push to main; the bridge and sealed-realm operations require explicit workflow dispatch. Recheck trigger boundaries before changing workflows or publishing a new branch with different deployment behavior.

Use the existing configured GitHub authentication. Example publication command (substitute the reviewed SHA):

```powershell
git push upstream REVIEWED_SHA:refs/heads/codex/prepared-keep-bindings-fix
git ls-remote upstream refs/heads/codex/prepared-keep-bindings-fix
```

This is a development procedure, not a scheduled background sync service. Commit and push at completed task/review checkpoints; never claim uncommitted work is already on GitHub.
